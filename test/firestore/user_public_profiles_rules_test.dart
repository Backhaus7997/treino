// `userPublicProfiles/{uid}` Firestore rules enforcement is now covered by a
// REAL emulator-backed test suite (not a skipped stub):
//   functions/src/__tests__/user-public-profiles-rules.test.ts
// Run with: firebase emulators:exec --only firestore,auth \
//   "npm --prefix functions run test:rules"
// This Dart file intentionally has no test content — it is superseded.
void main() {}
