# Tasks — home-shell

**Change**: `home-shell`  
**Fase / Etapa**: Fase 2 · Etapa 1  
**TDD**: Strict — every widget task pair is (a) RED test file → (b) GREEN widget file.  
**Delivery**: Single PR (`feat/home-shell`). Production ~340 LOC fits the 400-line budget.  
**Total tasks**: 21 (1 scaffold + 5 × 2 widget pairs + 1 integration pair + 3 quality gates)

---

## [x] TASK-001 — Bootstrap: test directories + pubspec dep

- **REQ refs**: infrastructure (implicit in REQ-HOME-HEADER-003: `cached_network_image` must be in pubspec)
- **Files**:
  - `pubspec.yaml` — add `cached_network_image: ^3.4.1` after `flutter_svg` line
  - `pubspec.lock` — regenerated by `flutter pub get`
  - `test/features/home/` — create directory (empty, git-tracked via `.gitkeep` or first test file)
  - `test/features/home/widgets/` — create directory
- **Done when**: `flutter pub get` exits 0; `flutter analyze` still reports 0 issues; `pubspec.lock` contains a top-level `cached_network_image` entry; both `test/features/home/` and `test/features/home/widgets/` directories exist in the working tree.
- **Notes**: Commit both `pubspec.yaml` and `pubspec.lock` together. Message: `chore(deps): add cached_network_image ^3.4.1`. Do NOT write any widget code in this task. If `^3.4.1` fails pub resolution against the project's Dart SDK `^3.5.0`, fall back to `^3.4.0` and document the deviation in apply-progress.

---

## [x] TASK-002a — HomeCTAButton: test file (RED)

- **REQ refs**: REQ-HOME-CTA-001, REQ-HOME-CTA-002, REQ-HOME-CTA-003, REQ-HOME-CTA-004, REQ-HOME-CTA-005
- **Files**:
  - `test/features/home/widgets/home_cta_button_test.dart` — new file (~80 LOC)
- **Done when**: file exists; `flutter test test/features/home/widgets/home_cta_button_test.dart` compiles but all 5 tests FAIL with "Target file not found" or import error (widget doesn't exist yet). The 5 tests cover: (1) label rendered, (2) tap fires callback, (3) null onPressed no crash, (4) style assertions (StadiumBorder + accent bg + Barlow Condensed w700), (5) leadingIcon present/absent.
- **Notes**: Use `_wrap(Widget w)` helper as established in `test/features/auth/` convention. Import will fail until TASK-002b creates the widget — that failure IS the RED state. Do not import from `home_cta_button.dart` yet if the file doesn't exist; structure the test so the missing import is the failure mode.

---

## [x] TASK-002b — HomeCTAButton: widget implementation (GREEN)

- **REQ refs**: REQ-HOME-CTA-001, REQ-HOME-CTA-002, REQ-HOME-CTA-003, REQ-HOME-CTA-004, REQ-HOME-CTA-005
- **Files**:
  - `lib/features/home/widgets/home_cta_button.dart` — new file (~70 LOC)
- **Done when**: `flutter test test/features/home/widgets/home_cta_button_test.dart` reports 5/5 green; `flutter analyze` 0 issues on this file.
- **Notes**:
  - API: `HomeCTAButton({required String label, VoidCallback? onPressed, IconData? leadingIcon})`.
  - Use `ElevatedButton` with `ElevatedButton.styleFrom(backgroundColor: palette.accent, foregroundColor: palette.bg, disabledBackgroundColor: palette.accent.withValues(alpha: 0.5), disabledForegroundColor: palette.bg, shape: const StadiumBorder())`. Pass `onPressed` through directly — Material handles disabled state automatically.
  - Text style: `GoogleFonts.barlowCondensed(fontWeight: FontWeight.w700, fontSize: 16, letterSpacing: 1.0, color: palette.bg)`.
  - Fixed height `56` via `SizedBox(height: 56, width: double.infinity, child: ElevatedButton(...))`.
  - `leadingIcon` renders `Icon(leadingIcon, color: palette.bg, size: 18)` + `SizedBox(width: 8)` before label text; when null, no `Icon` widget present.
  - No HEX literals. No `PhosphorIcons.*` direct references.
  - Commit: `feat(home): add HomeCTAButton widget`.

---

## [x] TASK-003a — EstaSemanaCard: test file (RED)

- **REQ refs**: REQ-HOME-SEMANA-001, REQ-HOME-SEMANA-002
- **Files**:
  - `test/features/home/widgets/esta_semana_card_test.dart` — new file (~60 LOC)
- **Done when**: file exists with 3 tests: (1) `find.text('ESTA SEMANA')` finds one, (2) `find.text('Todavía no entrenaste esta semana.')` finds one + absence of `RegExp(r'\d+ DÍAS')` + `find.byType(SvgPicture)` finds zero, (3) decoration assertions (bgCard + r=20 + border non-null). Tests FAIL (widget doesn't exist).
- **Notes**: `EstaSemanaCard` has no dependencies on other home widgets — this task is independent of TASK-002b. Can be done in parallel with TASK-002b if two devs work simultaneously; for a solo dev, follow the listed order.

---

## [x] TASK-003b — EstaSemanaCard: widget implementation (GREEN)

- **REQ refs**: REQ-HOME-SEMANA-001, REQ-HOME-SEMANA-002
- **Files**:
  - `lib/features/home/widgets/esta_semana_card.dart` — new file (~50 LOC)
- **Done when**: `flutter test test/features/home/widgets/esta_semana_card_test.dart` reports 3/3 green; `flutter analyze` 0 issues.
- **Notes**:
  - `class EstaSemanaCard extends StatelessWidget` with `const EstaSemanaCard({super.key})`. Zero constructor params.
  - Outer `Container` with `BoxDecoration(color: palette.bgCard, borderRadius: BorderRadius.circular(20), border: Border.all(color: palette.border, width: 1))`.
  - `Padding(padding: EdgeInsets.all(18), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text('ESTA SEMANA', ...), SizedBox(height: 12), Text('Todavía no entrenaste esta semana.', ...)]))`.
  - "ESTA SEMANA": `GoogleFonts.barlowCondensed(fontWeight: FontWeight.w700, fontSize: 14, letterSpacing: 1.4, color: palette.textPrimary)`.
  - Body text: `GoogleFonts.barlow(fontWeight: FontWeight.w400, fontSize: 13, color: palette.textMuted)`.
  - No SvgPicture, no streak number, no week dots.
  - Commit: `feat(home): add EstaSemanaCard placeholder`.

---

## [x] TASK-004a — EmpezarEntrenamientoCard: test file (RED)

- **REQ refs**: REQ-HOME-EMPEZAR-001, REQ-HOME-EMPEZAR-002, REQ-HOME-EMPEZAR-003, REQ-HOME-EMPEZAR-004
- **Files**:
  - `test/features/home/widgets/empezar_entrenamiento_card_test.dart` — new file (~80 LOC)
- **Done when**: file exists with 5 tests: (1) all 6 hardcoded strings present (`'HOY · JUEVES'`, `'PUSH'`, `'Pecho · Hombros · Tríceps'`, `'6 ejercicios'`, `'~55 min'`, `'EMPEZAR ENTRENAMIENTO'` — sin caracter `▶` adentro, ese va como `leadingIcon`), (2) icons `TreinoIcon.tabWorkout` and `TreinoIcon.clock` found, (3) decoration assertions (bgCard + r=20 + border), (4) tap no-op — no exception, no navigation, (5) `find.byType(HomeCTAButton)` finds one with `label == 'EMPEZAR ENTRENAMIENTO'` AND `leadingIcon == TreinoIcon.play`. Tests FAIL (widget doesn't exist).
- **Notes**: Depends on TASK-002b being complete so the import of `HomeCTAButton` resolves. Do NOT start this task until TASK-002b is green.

---

## [x] TASK-004b — EmpezarEntrenamientoCard: widget implementation (GREEN)

- **REQ refs**: REQ-HOME-EMPEZAR-001, REQ-HOME-EMPEZAR-002, REQ-HOME-EMPEZAR-003, REQ-HOME-EMPEZAR-004
- **Files**:
  - `lib/features/home/widgets/empezar_entrenamiento_card.dart` — new file (~110 LOC)
- **Done when**: `flutter test test/features/home/widgets/empezar_entrenamiento_card_test.dart` reports 5/5 green; `flutter analyze` 0 issues.
- **Notes**:
  - `class EmpezarEntrenamientoCard extends StatelessWidget` with `const EmpezarEntrenamientoCard({super.key})`. Zero params, no `ref.watch`/`ref.read`.
  - All strings as `static const` private fields so Etapa 5 can swap them with provider-driven data without renaming the widget.
  - Card shell: same `Container` + `BoxDecoration` pattern as `EstaSemanaCard` (bgCard, r=20, border 1px).
  - Internal spacing follows design §3 widget tree: gaps 8, 8, 14, 18 — all in the allowed set.
  - Stat row: `Row` with `Icon(TreinoIcon.tabWorkout, size: 16, color: palette.textMuted)` + `SizedBox(width: 8)` + `Text('6 ejercicios')` + `SizedBox(width: 18)` + `Icon(TreinoIcon.clock, size: 16)` + `SizedBox(width: 8)` + `Text('~55 min')`.
  - `HomeCTAButton(label: 'EMPEZAR ENTRENAMIENTO', leadingIcon: TreinoIcon.play, onPressed: null)`. **Importante**: el play va como `leadingIcon` (param del componente), no como caracter `▶` adentro del label string. El caracter Unicode renderiza como emoji naranja en Windows.
  - No `PhosphorIcons.*`. No HEX literals.
  - Commit: `feat(home): add EmpezarEntrenamientoCard with hardcoded content`.

---

## [x] TASK-005a — HomeHeader: test file (RED)

- **REQ refs**: REQ-HOME-HEADER-001, REQ-HOME-HEADER-002, REQ-HOME-HEADER-003, REQ-HOME-HEADER-004
- **Files**:
  - `test/features/home/widgets/home_header_test.dart` — new file (~100 LOC)
- **Done when**: file exists with 6 tests: (1) named greeting `'HOLA, MARTÍN!'`, (2) lowercase input uppercased `'HOLA, ANA!'`, (3) null profile → `'HOLA!'` + no `'HOLA, '` match, (4) non-null avatarUrl → `CachedNetworkImage` found, (5) null avatarUrl with displayName → initials `'M'` + no `CachedNetworkImage`, (6) null profile → `'?'` + no `CachedNetworkImage`. Tests FAIL (widget doesn't exist). Uses `makeProfile(...)` factory helper defined in the test file.
- **Notes**: `HomeHeader` depends only on `UserProfile` (value type, no mocks needed) and `cached_network_image` (TASK-001 must be complete). No dependency on other home widgets.

---

## [x] TASK-005b — HomeHeader: widget implementation (GREEN)

- **REQ refs**: REQ-HOME-HEADER-001, REQ-HOME-HEADER-002, REQ-HOME-HEADER-003, REQ-HOME-HEADER-004
- **Files**:
  - `lib/features/home/widgets/home_header.dart` — new file (~110 LOC)
- **Done when**: `flutter test test/features/home/widgets/home_header_test.dart` reports 6/6 green; `flutter analyze` 0 issues.
- **Notes**:
  - `class HomeHeader extends StatelessWidget` with `const HomeHeader({super.key, required this.profile})`. `final UserProfile? profile;`.
  - Import: `package:treino/features/profile/domain/user_profile.dart`.
  - Greeting: `(displayName != null && displayName.isNotEmpty) ? 'HOLA, ${displayName.toUpperCase()}!' : 'HOLA!'`. Style: `GoogleFonts.barlowCondensed(fontWeight: FontWeight.w700, fontSize: 28, letterSpacing: 0.8, color: palette.textPrimary)`.
  - Initials: `_computeInitials(displayName)` — `displayName.characters.first.toUpperCase()` or `'?'`. `String.characters` is part of Flutter's transitive dep; if `dart analyze` warns, add `import 'package:characters/characters.dart';`.
  - Avatar: `SizedBox(56×56) > ClipOval > (avatarUrl != null ? CachedNetworkImage(...) : _AvatarFallback(...))`.
  - `_AvatarFallback` is a private widget in this file: `Container(decoration: BoxDecoration(shape: BoxShape.circle, gradient: LinearGradient(colors: [palette.accent, palette.highlight])), alignment: Alignment.center, child: Text(initials, ...))`.
  - Header row: `Row(crossAxisAlignment: CrossAxisAlignment.center, mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [Expanded(child: greetingText), SizedBox(width: 12), avatarWidget])`.
  - Commit: `feat(home): add HomeHeader with greeting and avatar`.

---

## [x] TASK-006a — HomeScreen: test file (RED)

- **REQ refs**: REQ-HOME-SCREEN-001, REQ-HOME-SCREEN-002, REQ-HOME-SCREEN-003, REQ-HOME-PROVIDER-001, REQ-HOME-PROVIDER-002, REQ-HOME-PROVIDER-003, REQ-HOME-PROVIDER-004
- **Files**:
  - `test/features/home/home_screen_test.dart` — new file (~140 LOC)
- **Done when**: file exists with 7 tests listed below. Tests compile (all 4 home widgets exist) but the assertions against the NEW `HomeScreen` behaviour FAIL because `home_screen.dart` still has the old placeholder implementation.
  1. `AsyncData(profile)` → `HomeHeader` found 1×, `EmpezarEntrenamientoCard` found 1×, `EstaSemanaCard` found 1×.
  2. `AsyncLoading()` → `HomeHeader` found 0×, skeleton (`SizedBox` with height > 0) found where header should be; both cards found 1× each.
  3. `AsyncError(...)` → no FlutterError, `find.text('HOLA!')` finds 1, no error text matching `RegExp(r'[Ee]rror|[Ee]xcepci')`.
  4. No Scaffold/AppBackground/SafeArea introduced: `find.byType(Scaffold)` finds exactly 1 (outer test wrapper only), `find.byType(AppBackground)` finds 0, `find.byType(SafeArea)` finds 0.
  5. `AsyncData(profile)` → `tester.widget<HomeHeader>(...).profile` equals the overridden profile object.
  6. `AsyncData(null)` → `tester.widget<HomeHeader>(...).profile` is null.
  7. `AsyncData(profile)` with displayName + avatarUrl → `find.text('HOLA, MARTÍN!')` finds 1, `CachedNetworkImage` found ≥1, no FlutterError.
- **Notes**: Depends on TASK-002b, TASK-003b, TASK-004b, TASK-005b all being green — all four child widgets must exist before this test can compile. Use `_wrapWithOverrides` helper. Provider override pattern per design §8.2: `userProfileProvider.overrideWith(...)` returning a `Stream<UserProfile?>`. After each non-loading override, call `await tester.pump(const Duration(milliseconds: 50))`. For loading: single `tester.pump()` (no settle) to keep provider in `AsyncLoading`.

---

## [x] TASK-006b — HomeScreen: full rewrite (GREEN)

- **REQ refs**: REQ-HOME-SCREEN-001, REQ-HOME-SCREEN-002, REQ-HOME-SCREEN-003, REQ-HOME-PROVIDER-001, REQ-HOME-PROVIDER-002, REQ-HOME-PROVIDER-003, REQ-HOME-PROVIDER-004
- **Files**:
  - `lib/features/home/home_screen.dart` — full rewrite (~50 LOC production + `_HomeHeaderSkeleton` private widget)
- **Done when**: `flutter test test/features/home/home_screen_test.dart` reports 7/7 green; `flutter analyze` 0 issues on this file; the old centered-Column placeholder is gone (verified by `rg 'Tu home base' lib/` returning no match).
- **Notes**:
  - `class HomeScreen extends ConsumerWidget`. Single `ref.watch(userProfileProvider)` call.
  - `.when(data: (p) => HomeHeader(profile: p), loading: () => const _HomeHeaderSkeleton(), error: (_, __) => const HomeHeader(profile: null))` — error falls back silently.
  - `_HomeHeaderSkeleton` is private to this file: `class _HomeHeaderSkeleton extends StatelessWidget` returning `const SizedBox(height: 56)`. No animation.
  - Root widget returned from `build`: `Padding(padding: EdgeInsets.symmetric(horizontal: 20), child: ListView(padding: EdgeInsets.symmetric(vertical: 20), physics: ClampingScrollPhysics(), children: [headerOrSkeleton, SizedBox(height: 20), EmpezarEntrenamientoCard(), SizedBox(height: 12), EstaSemanaCard()]))`.
  - NO `Scaffold`, `AppBackground`, or `SafeArea` inside this widget or its subtree.
  - Comment at top of file: `// NOTE: Scaffold, AppBackground, and SafeArea are provided by _ShellScaffold in router.dart. Do NOT add them here.`
  - Commit: `feat(home): wire HomeScreen to userProfileProvider with 4 async branches`.

---

## [x] TASK-007 — Quality gate: dart format

- **REQ refs**: infrastructure (success criterion 5 from propose.md)
- **Files**: all new/modified files under `lib/features/home/` and `test/features/home/`
- **Done when**: `dart format .` exits 0 and produces no diff output (tree is already formatted). If there were formatting issues, they have been corrected and changes staged.
- **Notes**: Run AFTER TASK-006b. This is a mandatory pre-merge gate per AGENTS.md and `docs/workflow.md`. Do not combine with TASK-008 — run format first, then analyze, so format errors don't mask analyze output.

---

## [x] TASK-008 — Quality gate: flutter analyze

- **REQ refs**: infrastructure (success criterion 4 from propose.md)
- **Files**: whole repo (analyze is project-wide)
- **Done when**: `flutter analyze` reports `No issues found!` (0 errors, 0 warnings, 0 infos introduced by this PR). Additionally, manual grep confirms: no HEX literals (`rg '#[0-9a-fA-F]{6}' lib/features/home/`), no `PhosphorIcons\.` references (`rg 'PhosphorIcons\.' lib/features/home/`), no spacing values 16 or 24 (`rg 'SizedBox\(height: 1[6]' lib/features/home/` and similar for width/padding).
- **Notes**: Run AFTER TASK-007 (format first). Commit any fixups as `chore(home): format + analyze gate`.

---

## [~] TASK-009 — Quality gate: full test suite + smoke run (MANUAL-PENDING smoke)

- **REQ refs**: all REQs (final verification pass)
- **Files**: none (read-only verification)
- **Done when**:
  1. `flutter test` (full suite, not just home/) exits 0 — no regressions introduced in auth, profile, or other feature tests.
  2. `flutter test test/features/home/` exits 0 — all 26 home widget tests green.
  3. Manual `flutter run` (debug device or simulator): navigate to `/home` tab, verify: header shows real name + avatar (or fallback), "HOY · JUEVES / PUSH" card renders with CTA pill, "ESTA SEMANA" placeholder renders, no double AppBackground/double SafeArea/double Scaffold visible, CTA tap does nothing (no crash, no navigation).
- **Notes**: This is the final PR-ready gate. If any test is red at this point, it must be fixed before opening the PR. Document any smoke-run visual deviations from the mockup in apply-progress. The PR is not ready to open until this task is fully green.

---

## Dependency summary

```
TASK-001 (bootstrap)
  └─► TASK-002a (CTA test)
        └─► TASK-002b (CTA widget)
              ├─► TASK-004a (Empezar test)  ← also needs TASK-003b for imports? No — EmpezarCard imports CTA, not EstaSemana.
              │     └─► TASK-004b (Empezar widget)
              │           └─► TASK-006a (Screen test)  ← needs 002b + 003b + 004b + 005b
              │                 └─► TASK-006b (Screen rewrite)
              └─► TASK-005a (Header test)  ← needs TASK-001 for cached_network_image
                    └─► TASK-005b (Header widget)
                          └─► TASK-006a (see above)

TASK-001
  └─► TASK-003a (EstaSemana test)  ← independent of CTA
        └─► TASK-003b (EstaSemana widget)
              └─► TASK-006a (see above)

TASK-006b ─► TASK-007 ─► TASK-008 ─► TASK-009
```

**Parallelizable pairs** (two devs): TASK-003a/003b can run in parallel with TASK-002a/002b after TASK-001. TASK-005a can start after TASK-001 (no widget dep for test file). For a solo dev, the listed sequential order (001 → 002 → 003 → 004 → 005 → 006 → gates) is optimal.

---

## Review Workload Forecast

| Metric | Value |
|---|---|
| Estimated production LOC | ~340 (4 new widgets + HomeScreen rewrite + `_HomeHeaderSkeleton`) |
| Estimated test LOC | ~460 (5 test files, ~26 widget tests) |
| Estimated total diff | ~800 LOC across 11 files (9 new, 2 modified) |
| Files changed | 11 (`pubspec.yaml`, `pubspec.lock`, 4 widget files, `home_screen.dart`, 5 test files) |
| **400-line production budget risk** | **Low** — ~340 production LOC is comfortably under the 400-line threshold. All production code is mechanical widget composition: no logic, no algorithms, no ambiguous architecture decisions. |
| **Chained PRs recommended** | **No** — the 400-line budget rule applies to production code, not tests. ~340 production LOC fits in a single PR. Splitting into multiple PRs would create artificial dependencies (e.g. a PR with only `HomeCTAButton` that has no visible effect in the app) and increase reviewer overhead for no benefit. All 11 files are tightly coupled to the same visible screen — a reviewer needs to see the whole screen to verify visual parity. |
| **Decision needed before apply** | **No** — proceed directly to `sdd-apply` as a single PR. |

**Rationale for single PR decision**: the design's own §1 states "Estimated total diff: ~800 LOC across 11 files. Production code is ~340 LOC, tests are ~460 LOC. Well inside the 400-line production-code budget for a single PR; total stays reviewable because the surface is mechanical (widgets + tests, no logic)." The tasks decomposition confirms this assessment — there is no task that introduces architectural risk requiring isolated review. The highest-risk task (TASK-006b, HomeScreen rewrite) is fully covered by 7 widget tests before the reviewer sees the code.
