{
  "name": "treino-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules tests for posts and friendships",
  "scripts": {
    "test": "jest --testEnvironment node"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
