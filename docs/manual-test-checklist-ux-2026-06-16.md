# ✅ Checklist de pruebas manuales — UX HIGH (4 PRs)

Setup: emulador+seed (`npm --prefix scripts run seed:emulator`) · launch `--dart-define=USE_EMULATOR=true --dart-define=DEV_AUTH=athlete|coach` · es-AR · a11y→VoiceOver ON · errores→matá Firestore y reintentá. Pasos completos en `docs/manual-test-plan-ux-2026-06-16.md`.


## PR #171 — Accesibilidad (VoiceOver ON)

**P0**
- [ ] **A11Y-01** (either) Auth circle back button announces "Volver" and is 44x44
   ↳ _Esperá:_ VoiceOver announces "Volver" followed by the trait "botón" (button). The circle is rendered at 44x44 (bumped from 40), so it is a comfortable tap target with no accidental misses.
- [ ] **A11Y-07** (either) Chat: send button busy state announces "Enviando…"
   ↳ _Esperá:_ Idle: tooltip announces "Enviar" (l10n.chatScreenSendLabel). While sending: the control announces "Enviando…" (l10n.chatSendingA11y) and is reported disabled (Semantics enabled:false; tooltip also sw…
- [ ] **A11Y-09** (coach) Athlete detail plan card edit/delete: labels + 44x44 targets
   ↳ _Esperá:_ Edit announces "Editar plan, botón" (l10n.athleteDetailEditPlanA11y). Delete announces "Eliminar plan, botón" (l10n.athleteDetailDeletePlanA11y). Both now have constraints minWidth:44/minHeight:44 (w…
- [ ] **A11Y-16** (either) Create post header: CANCELAR/PUBLICAR are labeled buttons; PUBLICAR announces "Publicando…
   ↳ _Esperá:_ CANCELAR announces "Cancelar, botón" (l10n.commonCancel; visible glyph excluded). Title "NUEVO POST" announces with heading trait. PUBLICAR announces "Crear publicación, botón" and reports enabled/di…
- [ ] **A11Y-18** (athlete) Friend requests inbox: back button + ACEPTAR/RECHAZAR pills labeled and 44pt
   ↳ _Esperá:_ Back announces "Volver, botón" (l10n.commonBack) with a >=44pt hit box. RECHAZAR announces "RECHAZAR, botón" (l10n.dashboardRechazarLabel) and ACEPTAR announces "ACEPTAR, botón" (l10n.dashboardAcepta…
- [ ] **A11Y-28** (either) Routine editor: icon-only controls labeled and 44pt (back, delete day, reorder, slot menu…
   ↳ _Esperá:_ Back: "Volver" (tooltip commonBack). Day trash: "Eliminar día" (routineEditorDeleteDayA11y), now min 44x44 (was collapsed). Reorder chevrons: "Subir" / "Bajar" (routineEditorSlotMenuMoveUp / ...MoveD…
- [ ] **A11Y-29** (athlete) Session player: back, per-set check, technique info, timer, ABANDONAR labeled and 44pt
   ↳ _Esperá:_ Back: "Volver, botón" (commonBack), wrapped to min 44x44 around the 36px circle. Per-set check: "Marcar serie <n> como completada, botón" (sessionPlayerSetCompleteA11y(setNumber)), now a 44x44 box (w…

**P1**
- [ ] **A11Y-02** (either) Login/Register top IconButton back announces "Volver"
   ↳ _Esperá:_ VoiceOver announces "Volver, botón" (the tooltip l10n.commonBack is exposed as the semantic label). Activating it pops the route or goes home.
- [ ] **A11Y-04** (either) Login "register" inline link is a 44pt button announced by name
   ↳ _Esperá:_ VoiceOver announces the exact visible register-link text (l10n.authLoginRegisterLink) with the trait "botón". Double-tap navigates to /register. The hit area is wrapped in a ConstrainedBox(minHeight:…
- [ ] **A11Y-05** (either) Legal document title and section headings are navigable headings
   ↳ _Esperá:_ The title announces "Términos y Condiciones, encabezado" (header trait). Each section.heading is also exposed as a heading, so the Headings rotor lets the user jump section-to-section through the lon…
- [ ] **A11Y-06** (either) Chat: back button labeled, peer avatar announced by name
   ↳ _Esperá:_ Back button announces "Volver, botón" (tooltip l10n.commonBack). The avatar announces "Foto de perfil de <peer name>" with the image trait (a11yAvatarLabel(name)).
- [ ] **A11Y-10** (athlete) Trainers list: MAPA/LISTA and Presencial/Online expose selected state; disabled MAPA expl…
   ↳ _Esperá:_ Mode tabs announce as buttons with selected state ("seleccionado") for the active one. The toggle pills announce as buttons with selected state; the pill's icon is excluded from semantics and the vis…
- [ ] **A11Y-11** (coach) Coach Hub login email/password offer Keychain autofill
   ↳ _Esperá:_ The Email field advertises username/email autofill (AutofillHints.username + email) and the password field advertises password autofill (AutofillHints.password); both are inside an AutofillGroup, so…
- [ ] **A11Y-12** (coach) Coach Hub plan preview: warning icon labeled, athlete options are toggle buttons, assign…
   ↳ _Esperá:_ Warning icon announces "Atención" (semanticLabel l10n.commonWarning). The "Asignar manualmente" TextButton now has minimumSize Size(0,44) (shrinkWrap removed) so it is reliably tappable. Each athlete…
- [ ] **A11Y-13** (coach) Coach Hub dashboard: loading spinners and student avatars announced
   ↳ _Esperá:_ Section loading spinner announces "Cargando…" (l10n.commonLoading, liveRegion). Each tile avatar announces "Foto de perfil de <name>" with the image trait. The pending-request busy spinner announces…
- [ ] **A11Y-14** (coach) Coach Hub Alumnos roster renders (regression guard for routing change)
   ↳ _Esperá:_ /alumnos now renders AlumnosScreen (the real roster), NOT the previous "Próximamente" placeholder. Search filters by name, chips partition the roster (Activos and Con deuda are disjoint), and the row…
- [ ] **A11Y-15** (either) Feed header search and create-post are labeled 44pt buttons
   ↳ _Esperá:_ Search announces "Buscar, botón" (l10n.feedSearchA11y) and navigates to /feed/search. Create announces "Crear publicación, botón" (l10n.feedCreatePostA11y) and navigates to /feed/create. Both are wra…
- [ ] **A11Y-17** (either) Create post: privacy pills are 44pt toggle buttons; routine stub icon decorative
   ↳ _Esperá:_ Each privacy pill announces its visible label with button role and selected state ("seleccionado" for the active one), enabled per isEnabled; the visible label text is excluded so it isn't double-rea…
- [ ] **A11Y-19** (athlete) Public profile: hero avatar named, message stub disabled-labeled, load states announced
   ↳ _Esperá:_ Loading spinner announces "Cargando…" (l10n.commonLoading). Hero avatar announces "Foto de perfil de <displayName>" with image trait. MENSAJE stub announces "Mensaje (próximamente), botón" and is rep…
- [ ] **A11Y-20** (athlete) Search users: back and clear-field are labeled 44x44 buttons
   ↳ _Esperá:_ Back announces "Volver, botón" (l10n.commonBack) inside a 44x44 SizedBox. The clear button announces "Limpiar búsqueda, botón" (l10n.searchUsersClearA11y) inside a 44x44 box and clears the field when…
- [ ] **A11Y-21** (either) Home header avatar announced by name (or generic when unknown)
   ↳ _Esperá:_ During load the skeleton announces "Cargando…" (l10n.commonLoading, liveRegion) instead of a silent empty surface. Loaded avatar announces "Foto de perfil de <displayName>" (a11yAvatarLabel) with ima…
- [ ] **A11Y-23** (coach) Trainer home bell badge announces pending-request count
   ↳ _Esperá:_ The bell announces "<count> solicitudes pendientes" (l10n.homePendingRequestsA11y(count)) where count matches the visible badge; the inner Stack (bell icon + numeric badge) is ExcludeSemantics so the…
- [ ] **A11Y-24** (athlete) Profile screen: headings, avatar identity merge, labeled tiles, decorative dividers
   ↳ _Esperá:_ Header block and each section title announce with heading trait. Avatar card (MergeSemantics) announces the user's name/handle/gym as a single node. Tiles announce: "Mis ejercicios, botón"; "Cerrar s…
- [ ] **A11Y-25** (either) Workout back buttons (exercise detail, routine detail, my exercises, custom editor) label…
   ↳ _Esperá:_ All four announce "Volver, botón" (l10n.commonBack via tooltip on IconButton or Semantics label on the wrapped GestureDetector). My Exercises back is now an IconButton (44pt + canPop fallback to /pro…
- [ ] **A11Y-27** (athlete) Post-workout summary: close button labeled, mood emojis excluded
   ↳ _Esperá:_ Close announces "Cerrar, botón" (l10n.commonClose). The mood emoji row is wrapped in ExcludeSemantics, so VoiceOver does NOT read the five emoji names in sequence — the decorative, non-interactive ro…

**P2**
- [ ] **A11Y-03** (either) Login TREINO logo is hidden from VoiceOver (decorative)
   ↳ _Esperá:_ VoiceOver SKIPS the logo entirely — it never gains focus and is not announced (wrapped in ExcludeSemantics). Focus moves from the back button straight to the headline text. The adjacent headline stil…
- [ ] **A11Y-08** (either) Chat empty conversation uses localized title (no hardcoded literal)
   ↳ _Esperá:_ The empty-state title renders l10n.chatListEmptyTitle (localized), NOT the previous hardcoded "Sin mensajes todavía" literal. Verify the displayed string matches the es-AR value of chatListEmptyTitle.
- [ ] **A11Y-22** (either) Home decorative icons (streak flame, status dot) skipped by VoiceOver
   ↳ _Esperá:_ Both the 64px streak flame and the 8px status dot are wrapped in ExcludeSemantics, so VoiceOver never focuses or announces them; only the surrounding text is read.
- [ ] **A11Y-26** (either) My Exercises: header heading, decorative empty icon, card reads as one button
   ↳ _Esperá:_ "MIS EJERCICIOS" announces with heading trait. The empty-state sparkle icon is ExcludeSemantics (skipped). Each exercise card announces as one button with label = name plus muscle group joined by ",…
- [ ] **A11Y-30** (athlete) Workout list: routine overflow menu labeled; completed/template icons decorative
   ↳ _Esperá:_ The routine overflow PopupMenuButton announces "Opciones de rutina, botón" (tooltip workoutRoutineOptionsA11y) and opens its menu. The Historial completed check icon and the trainer-template tinted i…


## PR #172 — Estados / errores

**P0**
- [ ] **STATES-01** (athlete) Failed set-write in active session shows SnackBar + Reintentar WITHOUT destroying the ses…
   ↳ _Esperá:_ A floating SnackBar appears with the text 'No pudimos guardar la serie. Reintentá.' and an action button labelled 'Reintentar'. CRITICALLY: the session is NOT destroyed — the elapsed timer keeps runn…
- [ ] **STATES-02** (athlete) Finish session with failed write shows Reintentar and does NOT strand the user on a froze…
   ↳ _Esperá:_ Navigation to the summary does NOT happen. A floating SnackBar appears: 'No pudimos finalizar la sesión. Probá de nuevo.' with a 'Reintentar' action. The player stays usable — the timer is NOT frozen…
- [ ] **STATES-03** (athlete) Abandon session with failed write shows Reintentar and keeps the session alive
   ↳ _Esperá:_ The player does NOT navigate to /workout. A floating SnackBar shows 'No pudimos finalizar la sesión. Probá de nuevo.' with a 'Reintentar' action (same copy as finish). The session/timer stays alive a…
- [ ] **STATES-04** (either) Splash surfaces an auth error with Reintentar instead of freezing on the brand screen
   ↳ _Esperá:_ While resolving, a small 22x22 accent-coloured CircularProgressIndicator shows below the logo (not a frozen, indicator-less brand screen). On auth failure, the spinner is replaced by the muted text '…
- [ ] **STATES-05** (athlete) Publish a post shows a success SnackBar after returning to the feed
   ↳ _Esperá:_ The compose screen pops back to the feed AND a SnackBar with the text 'Post publicado.' is shown on the feed (delivered via the app-level ScaffoldMessenger captured before the await, so it survives t…
- [ ] **STATES-06** (either) Save Datos personales and Gimnasio show success SnackBars; failed gym save shows an error…
   ↳ _Esperá:_ Personal save (success): SnackBar 'Cambios guardados.' then the screen pops. Gym save (success): SnackBar 'Gimnasio actualizado.' then the screen pops. Gym save (failure): the screen does NOT pop, an…

**P1**
- [ ] **STATES-07** (athlete) Feed segments show a localized load error with a working Reintentar that refetches
   ↳ _Esperá:_ Each errored segment shows centered muted text 'No pudimos cargar tu feed. Probá de nuevo.' with a 'Reintentar' TextButton below it. Because the feed providers are FutureProviders (they do NOT self-h…
- [ ] **STATES-08** (athlete) Accept/Reject friend request failure shows an error SnackBar instead of a silently unchan…
   ↳ _Esperá:_ On a failed accept or reject, the row stays in place (the stream emits no removal) AND a SnackBar shows 'No pudimos completar la acción. Probá de nuevo.' — the action is no longer silently swallowed.…
- [ ] **STATES-09** (athlete) Public profile SEGUIR / ACEPTAR shows in-flight spinner and success/error SnackBars
   ↳ _Esperá:_ During the in-flight write the pill shows a small inline spinner in place of its leading icon and is disabled (cannot double-fire). On a successful SEGUIR, a SnackBar shows 'Solicitud enviada.' On a…
- [ ] **STATES-10** (coach) Coach Hub dashboard sections all surface load errors with Reintentar (pending queue no lo…
   ↳ _Esperá:_ Every section (including the pending accept/decline queue) shows the error text 'No pudimos cargar esta sección.' with a 'Reintentar' TextButton — none of them silently collapses to empty. After conn…
- [ ] **STATES-11** (coach) Availability editor: loading spinner, error+Reintentar, and save success/error SnackBars
   ↳ _Esperá:_ Initial load shows a centered accent CircularProgressIndicator (not the empty-state hints). On stream error the body shows 'No pudimos cargar los entrenadores.' with a 'Reintentar' OutlinedButton tha…
- [ ] **STATES-12** (athlete) Insights screen error shows a localized message with a working Reintentar
   ↳ _Esperá:_ The error state shows centered muted text 'No pudimos cargar tus insights. Probá de nuevo.' with a 'Reintentar' TextButton beneath it. Tapping 'Reintentar' invalidates weeklyInsightsProvider; with co…
- [ ] **STATES-13** (athlete) Create-post screen load error is a designed state with Reintentar (not raw 'Error: ...')
   ↳ _Esperá:_ The error state shows a warning icon, the message 'No pudimos abrir el editor. Probá de nuevo.', and two tappable actions: 'CANCELAR' (pops back to the feed) and 'REINTENTAR' (invalidates createPostN…
- [ ] **STATES-14** (either) Coach Hub 'not allowed' sign-out shows a spinner and an error on failure (web)
   ↳ _Esperá:_ While signing out, the button is disabled and shows an 18x18 spinner in place of its icon (no double-tap possible). On failure, an inline danger-coloured message 'No pudimos cerrar sesión. Probá de n…
- [ ] **STATES-15** (either) Welcome screen surfaces social sign-in failures and shows a per-button spinner
   ↳ _Esperá:_ While the tapped provider's OAuth call is in flight, that specific button swaps its brand glyph for a small spinner and the other auth controls (incl. the primary CTA) are disabled. On failure or can…
- [ ] **STATES-16** (either) Forgot-password generic failure now renders the AuthFailureBanner
   ↳ _Esperá:_ The spinner stops AND an AuthFailureBanner renders showing the generic failure (driven by AuthFailure.unknown('reset-failed')) — the form no longer looks idle as if nothing happened.
- [ ] **STATES-17** (athlete) Post-workout summary disables COMPARTIR and shows a spinner while sharing
   ↳ _Esperá:_ While sharing (postWorkoutNotifierProvider.isLoading), the COMPARTIR button is disabled and shows a 16x16 accent spinner next to the text 'Cargando…'. The 'LISTO' button and the close (X) control are…
- [ ] **STATES-18** (either) Routine editor gives a SPECIFIC message for the first unmet requirement and scrolls to it
   ↳ _Esperá:_ The save button is always tappable and the SnackBar names the FIRST unmet requirement in form-fill order: (1) no name → 'Poné un nombre a la rutina.' (no scroll); (2) name present but a day lacks an…

**P2**
- [ ] **STATES-19** (either) Empty states render where a screen previously looked half-rendered
   ↳ _Esperá:_ Agenda: below the calendar a centered calendar icon + muted text 'Tu PF todavía no te agendó sesiones.' is shown (no longer a blank tail). Session detail: where the exercise blocks would be, a center…
- [ ] **STATES-20** (coach) Custom exercise editor (edit mode) shows loading/error states instead of degrading to a b…
   ↳ _Esperá:_ In edit mode while loading: a centered accent spinner inside the editor scaffold (not empty fields with 'EDITAR/GUARDAR CAMBIOS' chrome). On error: an inline error state with a visible message 'No pu…


## PR #174 — Usabilidad

**P0**
- [ ] **USABILITY-01** (either) Login bloquea email con formato invalido antes de la llamada de red
   ↳ _Esperá:_ El submit se BLOQUEA sin disparar ninguna llamada a Firebase (no aparece spinner de red ni el banner de error de servidor). Debajo del campo de email aparece el mensaje de validacion inline de EmailP…
- [ ] **USABILITY-04** (athlete) Setup de perfil paso 1: username ya tomado bloquea SIGUIENTE con 'Ese username ya esta en…
   ↳ _Esperá:_ Mientras consulta aparece la fila inline 'Verificando disponibilidad…' con spinner. Al resolver, aparece la fila roja (palette.danger) con icono de warning y el texto exacto 'Ese username ya esta en…
- [ ] **USABILITY-05** (athlete) Setup de perfil paso 1: username libre muestra 'Username disponible' y habilita SIGUIENTE
   ↳ _Esperá:_ Aparece 'Verificando disponibilidad…' con spinner y luego la fila en color accent con icono check y el texto exacto 'Username disponible'. El boton SIGUIENTE queda HABILITADO. Al tocarlo, avanza al p…
- [ ] **USABILITY-08** (athlete) Home first-run: atleta sin rutina ve la tarjeta 'Arranca tu entrenamiento' en vez del wor…
   ↳ _Esperá:_ En lugar de la EmpezarEntrenamientoCard hardcodeada (que mostraba 'HOY · JUEVES', 'PUSH', '6 ejercicios', '~55 min'), se ve la _AthleteFirstRunCard con: titulo 'Arranca tu entrenamiento', cuerpo 'Cre…
- [ ] **USABILITY-11** (either) Editor de rutina: editar y tocar back muestra '¿Descartar cambios?'
   ↳ _Esperá:_ Aparece un AlertDialog con titulo exacto '¿Descartar cambios?', cuerpo 'Si salis ahora vas a perder los cambios sin guardar.' y dos acciones: 'Cancelar' (textMuted) y 'Descartar' (en color danger). '…
- [ ] **USABILITY-12** (either) Editor de rutina: gesto de swipe-back (iOS) tambien dispara el guard de descarte
   ↳ _Esperá:_ El PopScope intercepta el pop implicito (canPop = !_isDirty) y, en vez de salir, aparece el mismo dialogo '¿Descartar cambios?'. Si se elige 'Cancelar', permanece en el editor; si 'Descartar', sale.
- [ ] **USABILITY-16** (coach) Log de medicion: valor no numerico se rechaza inline (no se descarta en silencio)
   ↳ _Esperá:_ Las letras no se pueden tipear (se filtran al ingresar). Para un valor con multiples separadores como '12.3.4', al tocar GUARDAR aparece el error inline bajo el campo: 'Ingresa un numero valido' (log…
- [ ] **USABILITY-19** (coach) Log de medicion: GUARDAR deshabilitado con formulario vacio; no se persiste record nulo
   ↳ _Esperá:_ Con todos los campos vacios, GUARDAR esta DESHABILITADO (canSave = trainerUid != null && !_saving && _hasValue, y _hasValue es false). Al cargar un valor se habilita en vivo; al borrarlo vuelve a des…
- [ ] **USABILITY-20** (coach) Log de evaluacion de rendimiento: valor invalido se rechaza inline y vacio deshabilita Gu…
   ↳ _Esperá:_ Con el formulario vacio, Guardar esta DESHABILITADO (canSave = ... && _hasAnyMetric; _hasAnyMetric false). Las letras se filtran al ingresar (FilteringTextInputFormatter [0-9.,]). Un valor no parseab…

**P1**
- [ ] **USABILITY-02** (either) Login con email valido + password no vacio pasa la validacion y procede
   ↳ _Esperá:_ La validacion de formato pasa (no aparece error inline bajo el email) y el login procede normalmente (spinner y luego Home). El cambio no rompe el happy-path.
- [ ] **USABILITY-03** (either) Olvidaste tu contrasena bloquea email invalido antes de enviar el link
   ↳ _Esperá:_ El submit se BLOQUEA por la validacion inline (EmailPasswordValidator.validateEmail) y NO se dispara la llamada de reset. No aparece el estado de exito (_sent). El mensaje de validacion se muestra de…
- [ ] **USABILITY-06** (athlete) Setup de perfil paso 1: formato invalido resetea disponibilidad a oculto y no consulta
   ↳ _Esperá:_ La fila de estado de disponibilidad DESAPARECE (vuelve a SizedBox.shrink, estado unknown) en cuanto el formato es invalido, y aparece el error de formato del validator del campo (p.ej. 'Solo letras,…
- [ ] **USABILITY-09** (athlete) Home first-run: CTA 'CREAR RUTINA' navega al editor de rutina
   ↳ _Esperá:_ Navega (push) al editor de rutina propia, ruta '/workout/my-routine-editor'. Como es push, el back vuelve a Home.
- [ ] **USABILITY-10** (athlete) Home first-run: CTA 'Buscar entrenador' navega a la pestana Coach
   ↳ _Esperá:_ Navega a la pestana/seccion Coach, ruta '/coach' (via context.go), donde el atleta explora y pide un entrenador.
- [ ] **USABILITY-13** (either) Editor de rutina: guardar exitoso NO dispara el dialogo de descarte
   ↳ _Esperá:_ El guardado persiste y el editor se cierra DIRECTAMENTE (pop/go) SIN mostrar '¿Descartar cambios?'. No hay falso positivo del guard tras un guardado exitoso.
- [ ] **USABILITY-14** (either) Feed: pull-to-refresh recarga el segmento con contenido
   ↳ _Esperá:_ Aparece un RefreshIndicator en color accent (palette.accent) y al soltar se re-ejecuta la query (ref.refresh del FutureProvider del segmento). La lista se actualiza al completar.
- [ ] **USABILITY-17** (coach) Log de medicion: valor fuera de rango (>500) se rechaza con 'El valor esta fuera de rango'
   ↳ _Esperá:_ Error inline bajo el campo: 'El valor esta fuera de rango' (logFieldOutOfRange) y el guardado se bloquea. El limite superior es 500 (_kMaxMetricValue) y el piso es 0; valores negativos tambien se rec…
- [ ] **USABILITY-21** (athlete) CTA de PF: atleta con vinculo a OTRO entrenador ve la linea de ayuda explicando el bloqueo
   ↳ _Esperá:_ El boton primario aparece deshabilitado con el label 'YA TENES UN PF', y AHORA debajo hay una linea de ayuda (textMuted, centrada, con liveRegion para VoiceOver) con el texto: 'Solo podes tener un PF…
- [ ] **USABILITY-22** (coach) Coach Hub preview de plan: salir tras mapear ejercicios manualmente pide confirmacion
   ↳ _Esperá:_ Aparece un AlertDialog con titulo '¿Salir sin guardar el plan?', cuerpo 'Vas a perder los ejercicios que mapeaste manualmente.', accion 'Cancelar' (textMuted) y accion 'Salir igual' (danger). 'Cancel…

**P2**
- [ ] **USABILITY-07** (athlete) Setup de perfil paso 1: al volver al paso con username precargado, la disponibilidad se r…
   ↳ _Esperá:_ Al re-entrar con username precargado se dispara la verificacion automaticamente (postFrameCallback -> updateUsername(initial)): se ve brevemente 'Verificando disponibilidad…' y luego 'Username dispon…
- [ ] **USABILITY-15** (either) Feed: pull-to-refresh funciona tambien sobre el estado vacio
   ↳ _Esperá:_ El estado vacio es desplazable (envuelto en _scrollableEmptyState con AlwaysScrollableScrollPhysics) y el pull-to-refresh dispara el RefreshIndicator igual que con contenido. Los textos de vacio exac…
- [ ] **USABILITY-18** (coach) Log de medicion: error en circunferencia colapsada fuerza apertura de la seccion
   ↳ _Esperá:_ El guardado detecta el valor invalido en la seccion colapsada (_isMetricInvalid sobre los _circumferenceCtrls), fuerza la expansion de la seccion para que el error inline sea visible, y luego bloquea…
- [ ] **USABILITY-23** (either) Feed: anuncio de accesibilidad del pull-to-refresh con VoiceOver
   ↳ _Esperá:_ VoiceOver anuncia la etiqueta semantica 'Desliza para actualizar' (feedPullToRefreshA11y) sobre la region del RefreshIndicator.


## PR #177 — Navegación

**P0**
- [ ] **NAVIGATION-01** (athlete) Messages icon in feed header opens the chat inbox (MENSAJES)
   ↳ _Esperá:_ A new screen pushes in with a transparent AppBar titled 'MENSAJES' (uppercase, condensed font) and a back arrow on the left. The body shows either the list of the user's chats (avatar + name + last m…
- [ ] **NAVIGATION-04** (athlete) Friend-requests bell with pending badge opens the inbox
   ↳ _Esperá:_ The badge shows the pending count (or '9+' if more than 9). Tapping pushes the friend-requests inbox. Header reads 'Solicitudes de amistad' (the inbox header), and the list shows the pending request…
- [ ] **NAVIGATION-06** (athlete) Public profile (from search) now has a back button that returns
   ↳ _Esperá:_ The public profile opens inside a transparent Scaffold+AppBar with a visible back arrow (tooltip 'Volver'). Tapping it pops back to the search results screen with the previous query intact. The profi…
- [ ] **NAVIGATION-09** (athlete) 'Ver todo' in the history section opens the full HISTORIAL list
   ↳ _Esperá:_ A full-screen list pushes in with its own back arrow and header 'HISTORIAL' (uppercase). It lists ALL completed sessions (uncapped), each row showing a check icon, the routine name, the formatted dat…
- [ ] **NAVIGATION-12** (coach) 'Mis horarios' button in the coach AGENDA tab opens the availability editor
   ↳ _Esperá:_ The availability editor pushes in, titled 'Mis horarios'. The trainer can configure bookable hours without leaving the AGENDA surface.
- [ ] **NAVIGATION-14** (coach) Coach Hub web: browser Back from preview returns to upload with file state intact
   ↳ _Esperá:_ The app returns to the upload step (/upload-plan) with the previously picked file still in state (the upload step shows the parsed file, not a blank picker). The dashboard→upload→preview transitions…

**P1**
- [ ] **NAVIGATION-02** (athlete) Chat inbox back arrow returns to the Feed
   ↳ _Esperá:_ The inbox closes and the user returns to the Feed tab with the feed scroll position intact. The bottom tab bar is back on the Feed tab.
- [ ] **NAVIGATION-03** (athlete) Chat inbox row opens the 1-1 chat thread
   ↳ _Esperá:_ The 1-1 ChatScreen for that conversation pushes over the inbox. Tapping back returns to the MENSAJES inbox (not directly to the Feed), because the row uses a nested Navigator.push.
- [ ] **NAVIGATION-05** (either) Friend-requests bell shows NO badge when there are zero pending requests
   ↳ _Esperá:_ No numeric badge is rendered on the bell (badge only appears when pendingRequestCount > 0). Tapping still opens the friend-requests inbox, which shows 'No hay solicitudes pendientes'.
- [ ] **NAVIGATION-07** (athlete) Public profile back button returns to Feed when opened from a post author
   ↳ _Esperá:_ Public profile opens with the back arrow visible. Tapping back returns the user to the Feed. Note the feed author tap uses context.go (not push), so the back arrow falls back to context.go('/feed') r…
- [ ] **NAVIGATION-10** (athlete) 'Ver todo' is hidden when there is no completed history
   ↳ _Esperá:_ Only the 'HISTORIAL' heading is shown; the 'Ver todo' button is NOT rendered (it appears only when completedCount > 0). The empty state for the section stays clean.
- [ ] **NAVIGATION-11** (athlete) Full HISTORIAL list: back arrow returns and a row opens session detail
   ↳ _Esperá:_ Tapping a row pushes the session detail (/workout/historial/{sessionId}). Back from detail returns to the HISTORIAL list. The list's back arrow then pops to the Workout tab (or falls back to /workout…
- [ ] **NAVIGATION-13** (coach) Availability editor back returns to the AGENDA tab
   ↳ _Esperá:_ The editor pops and the coach returns to the AGENDA tab (not to Profile), with the monthly calendar and the 'NUEVA SESIÓN' + clock action row still in place. This keeps 'configure when I'm bookable'…
- [ ] **NAVIGATION-15** (coach) Coach Hub web: preview 'Volver' and upload back arrow pop the stack
   ↳ _Esperá:_ The preview's back pops to the upload step (picked file retained), and the upload header back arrow pops to the dashboard. Both use canPop() ? pop() : go(fallback), so a normal in-flow back is a pop;…

**P2**
- [ ] **NAVIGATION-08** (athlete) Public profile reachable from a friend-request tile and back works
   ↳ _Esperá:_ The requester's public profile opens with a visible back arrow; tapping back pops to the friend-requests inbox.
- [ ] **NAVIGATION-16** (coach) Coach Hub web: successful import still lands on the dashboard (terminal go)
   ↳ _Esperá:_ On success the app navigates to the dashboard (/dashboard). This terminal transition intentionally still uses context.go (not push), so the back stack does not contain the just-completed upload/previ…
- [ ] **NAVIGATION-17** (athlete) VoiceOver announces the new feed header icon-only actions
   ↳ _Esperá:_ The bell announces 'Solicitudes de amistad, 2 pendientes' (count variant) when there are pending requests, or 'Solicitudes de amistad' when none. The messages icon announces 'Mensajes'. Both are expo…
